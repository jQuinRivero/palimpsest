[project]
name = "palimpsest-docs-site"
version = "0.1.0"
description = "User guide and architecture documentation for palimpsest."
requires-python = ">=3.12"
dependencies = [
    "jupyter-book>=1.0,<2",
    "sphinxcontrib-mermaid>=1.0,<2",
    "sphinx-sitemap>=2.6,<3",
]

[tool.uv]
package = false
